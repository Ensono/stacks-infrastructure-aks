# Pre-commit hooks for Ensono Stacks AKS Infrastructure
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # YAML Linting
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [-c=yamllint.conf]
        types: [yaml]

  # Terraform formatting and validation
  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.88.4
    hooks:
      - id: terraform_fmt
        args:
          - --args=-recursive
      - id: terraform_validate
        args:
          - --hook-config=--retry-once-with-cleanup=true

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: ^deploy/helm/values/
      - id: end-of-file-fixer
        exclude: ^deploy/helm/values/
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: check-merge-conflict
      - id: check-yaml
        args: [--allow-multiple-documents]
        exclude: ^(deploy/helm/(charts|values)/|deploy/tests/vendor/|build/azDevOps/azure/pipeline-vars\.template\.yml)
      - id: detect-private-key
        exclude: ^deploy/terraform/certs/

  # Markdown linting
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.39.0
    hooks:
      - id: markdownlint
        args: [--fix, --disable, MD013]
        exclude: ^(docs/|deploy/tests/vendor/)
        files: \.md$
